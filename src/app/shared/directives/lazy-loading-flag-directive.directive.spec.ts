import { LazyLoadingFlagDirectiveDirective } from './lazy-loading-flag-directive.directive';

describe('LazyLoadingFlagDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadingFlagDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
